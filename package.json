{
  "scripts": {
    "clean": "lerna run clean",
    "build": "lerna run build",
    "watch": "lerna run watch --parallel",
    "postinstall": "lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^2.9.0"
  }
}
