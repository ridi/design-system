{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "lerna run clean",
    "build": "lerna run build",
    "start": "lerna run watch --parallel"
  },
  "devDependencies": {
    "lerna": "^3.4.3"
  },
  "engines": {
    "node": ">=10"
  }
}
