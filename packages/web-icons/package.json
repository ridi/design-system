{
  "name": "@ridi/web-icons",
  "version": "0.0.0",
  "description": "RIDI Web Icons",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ridi/design-system.git"
  },
  "main": "index.js",
  "scripts": {
    "svgo": "svgo --config=.svgo.yml --folder=svgs",
    "build": "node build.js",
    "watch": "chokidar 'svgs/*.svg' -c 'node build.js' --initial --silent",
    "precommit": "lint-staged"
  },
  "devDependencies": {
    "chalk": "^2.4.1",
    "chokidar-cli": "^1.2.1",
    "lint-staged": "^8.1.0",
    "svgo": "^1.1.1"
  }
}
